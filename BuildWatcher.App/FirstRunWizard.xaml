<Window x:Class="BuildWatcher.App.FirstRunWizard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BuildWatcher Setup"
        Height="220"
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Select NAS Root Folder"
                   FontSize="14"
                   FontWeight="Bold"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBox x:Name="PathBox"
                     Width="360"
                     IsReadOnly="True"/>
            <Button Content="Browse"
                    Margin="8,0,0,0"
                    Click="Browse_Click"/>
        </StackPanel>

        <Button Grid.Row="2"
                Content="Finish Setup"
                Height="32"
                Margin="0,20,0,0"
                HorizontalAlignment="Right"
                Width="120"
                Click="Finish_Click"/>
    </Grid>
</Window>
